#include "holberton.h"
/**
 * _isupper - returns 1 if is c uppercase character
 * @c: character to check for uppercase
 *
 * Return: returns 1 if is a uppercase character, returns 0 otherwise
 */
int _isupper(int c)
{
	if (c >= 'a' && c <= 'z')

	return (1);

else

	return (0);
}
