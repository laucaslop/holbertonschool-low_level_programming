#include "holberton.h"
/**
 * _islower - returns 1 if is a lowercase character
 * @c: character to check for lowercase
 *
 * Return: returns 1 if is a lowercase character, returns 0 if not
 */
int _islower(int c)
{
	if (c >= 'a' && c <= 'z')

	return (1);

else

	return (0);
}
