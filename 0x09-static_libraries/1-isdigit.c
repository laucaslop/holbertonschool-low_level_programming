#include "holberton.h"

/**
 * _isdigit - returns 1 if c is a digit
 * @c: character to check for lowercase
 *
 * Return: returns 0 otherwise
 */

int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
