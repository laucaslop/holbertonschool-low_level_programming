#include <stdio.h>

/**
 * main - prints whether a random number is postive or negative
 * Return: 0
 */
int main(void)

{
	char c;

	for (c = 'A' ; c <= 'Z' ; c++) {

	putchar (c);
}

	return (0);
}
