#include <stdio.h>

/**
 * main - prints whether a random number is postive or negative
 * Return: 0
 */

int main(void)
{
	char c = 'z';

	while
	(c >= 'a')
	{
		putchar (c);
		c--;
	}
	putchar('\n');

	return (0);
}
